Тут лежат скрипты для создания таблиц и импорта данных из базы ФИАС.


Python-скрипты для создания sql-скрипта импорта данных из базы ФИАС из формата DBF:

fias_import.py - импорт таблицы адресных объектов (addrobj)
fias_import_house.py - импорт таблицы домов (house)
    Данные скрипты пишут sql команды insert с данными на STDOUT - перенаправляем в нужный файл.
    В самих скпритах прописан путь до каталога с распакованными данными.
    Они используют либу dbfread.(ещё typing используют, но это можно и выпилить)

fias_init.sql - создание таблицы адресов.
fias_init_house.sql - создание таблицы домов.

fias_select.sql - запрос для рекурсивного получения полного адреса - но он только по одному элементу - для полнотекстового надо что-то ещё делать. + недоделанные запросы

Последовательность использования:
Базу надо ручками выкачать с http://fias.nalog.ru/Updates.aspx там в доке есть ссылка на актуальную например http://fias.nalog.ru/Public/Downloads/Actual/fias_dbf.rar
    (можно wget'ом c докачкой)
И распаковать + в python-скрипте путь этот указать.
Создать таблицы скриптами fias_init*
Натравить fias_import.py на каталог с базой распакованной, записав вывод в sql'ку. Выполнить полученную sql на нужной базе.


По самой структуре базы на хабре есть статьи, например https://habrahabr.ru/post/316314/
